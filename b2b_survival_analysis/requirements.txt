# Core (all KKBox/Telco analyses run with these alone)
pandas>=2.0.0
numpy>=1.24.0
matplotlib>=3.7.1

# ML baselines (kkbox_ml_baselines.py)
scikit-learn>=1.3.0
xgboost>=2.0.0        # optional; requires `brew install libomp` on macOS

# Legacy synthetic-data pipeline only (run_all_models.py et al., deprecated)
scikit-survival>=0.22.1
lifelines>=0.27.7
